
 [x] Consider the bias in orderbook samples (centered at the lowest ask price) in the algorithm generating synthetic samples.
 [x] Remove obsolete PriorityModeCount
 [x] Refactor selection of priority samples by using relative distribution tail values rather than absolute ones
 [x] In TkTImeSeriesForecasterTraining - change legend 'Accuracy' to 'KL-Div'
 [x] Make price history input in time series model to be discrete and use min_price_step of a share as a discretization value.
 [x] Rename PriorityTailThreshold to PriorityTailMeanThreshold
 [x] Generate orderbooks in reduced range { random(-width/2,0) ... random(0,width/2) } to improve likelihood to the real orderbooks with large min_price_step
 [x] TimeSeriesPreprocessor should shift the last trades samples in input history according to the pivot price
 [x] Try residual layers in time series prediction
 [x] Try lesser amount of future/predicted quants (2, 1) and observe the accuracy of the model
 [ ] Gather data over the whole day period, do not split it to Day and Evening.
 [ ] Refactor prices, volatilities and market regimes to be based on trades snapshots, not on instant last price values from LOBs
 [ ] Try fixed forecasting output, independent on min_price_increment. Since regime is not depending on it either.
